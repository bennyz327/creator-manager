export type schema_creators = {
    id: number;
    start_at: string;
    end_at: string;
}

export type schema_creator_identities = {
    creator_id: number;
    display_name: string;
    scoped_id: number;
    scoped_name: string;
}

export type schema_product_types = {
    id: number;
    name: string;
}